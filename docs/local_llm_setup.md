# Настройка локальной LLM модели

## Обзор

Приложение Support Assistant теперь поддерживает работу с локальными LLM моделями через Ollama в качестве альтернативы Perplexity API. Это позволяет:

- Работать полностью оффлайн
- Не тратить средства на API запросы
- Иметь полный контроль над моделью и данными
- Использовать различные open-source модели

## Установка Ollama

### macOS

```bash
# Установка через Homebrew
brew install ollama

# Или скачайте с официального сайта
# https://ollama.ai/download
```

### Linux

```bash
curl -fsSL https://ollama.ai/install.sh | sh
```

### Windows

Скачайте установщик с https://ollama.ai/download

## Загрузка модели qwen3:8b

После установки Ollama, загрузите модель:

```bash
# Загрузка и запуск модели qwen3:8b
ollama run qwen3:8b

# Модель загрузится и будет готова к использованию
# Можете протестировать её в интерактивном режиме
# Для выхода нажмите Ctrl+D или введите /bye
```

## Альтернативные модели

Вы можете использовать другие модели, доступные в Ollama:

```bash
# Llama 3 (8B параметров)
ollama run llama3

# Mistral (7B параметров)
ollama run mistral

# Phi-3 (3.8B параметров, более легкая)
ollama run phi3

# Просмотр всех доступных моделей
ollama list
```

## Конфигурация приложения

### 1. Выбор провайдера LLM

Отредактируйте `config/api_keys.yaml`:

```yaml
# Измените на "local" для использования локальной модели
llm_provider: "local"  # или "perplexity"

perplexity:
  api_key: "your-api-key-here"
```

### 2. Настройка локальной модели

Отредактируйте `config/local_llm_config.yaml`:

```yaml
# Модель для chat
chat_model:
  host: "localhost"
  port: 11434
  model_name: "qwen3:8b"  # Измените на другую модель при необходимости
  temperature: 0.7  # 0.0 = детерминированный, 1.0 = креативный
```

## Запуск приложения

### С локальной моделью

1. Убедитесь, что Ollama запущен:

```bash
# Проверка статуса
ollama list

# Если Ollama не запущен, запустите его
ollama serve
```

2. Запустите приложение:

```bash
cd /path/to/ticketManager
source venv/bin/activate
python src/main.py
```

При запуске вы увидите сообщение:

```
[LLM] Используется локальная модель: qwen3:8b на localhost:11434
```

### Переключение обратно на Perplexity

Измените в `config/api_keys.yaml`:

```yaml
llm_provider: "perplexity"
```

## Производительность

### Скорость генерации

Локальные модели работают медленнее, чем облачные API:

- **qwen3:8b**: ~10-30 токенов/сек (зависит от железа)
- **Perplexity API**: ~50-100 токенов/сек

### Требования к ресурсам

| Модель | RAM | VRAM (GPU) | Размер |
|--------|-----|------------|--------|
| qwen3:8b | 8 GB | 6 GB | ~4.7 GB |
| llama3:8b | 8 GB | 6 GB | ~4.7 GB |
| mistral:7b | 8 GB | 5 GB | ~4.1 GB |
| phi3:3.8b | 4 GB | 3 GB | ~2.3 GB |

## Устранение неполадок

### Ошибка подключения

```
[LLM] ПРЕДУПРЕЖДЕНИЕ: Модель qwen3:8b недоступна!
```

**Решение:**

1. Проверьте, что Ollama запущен:
   ```bash
   ollama list
   ```

2. Загрузите модель, если она отсутствует:
   ```bash
   ollama run qwen3:8b
   ```

3. Проверьте, что порт 11434 не занят:
   ```bash
   lsof -i :11434
   ```

### Медленная генерация

**Решения:**

1. Используйте более легкую модель (phi3)
2. Убедитесь, что используется GPU (если доступен)
3. Закройте другие приложения, потребляющие память

### Модель не отвечает

```
Таймаут при генерации ответа
```

**Решение:**

1. Первый запрос может быть медленным (модель загружается в память)
2. Увеличьте таймаут в коде (по умолчанию 120 секунд)
3. Проверьте логи Ollama:
   ```bash
   ollama logs
   ```

## Сравнение: Perplexity vs Локальная модель

| Критерий | Perplexity | Локальная (qwen3:8b) |
|----------|------------|----------------------|
| Скорость | ⚡⚡⚡ Быстро | ⚡⚡ Средне |
| Стоимость | 💰 Платно | 🆓 Бесплатно |
| Качество | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Оффлайн | ❌ Нет | ✅ Да |
| Приватность | ⚠️ Данные уходят в облако | ✅ Всё локально |
| Настройка | ✅ Простая | ⚙️ Требует установки |

## Рекомендации

- **Для продакшена**: используйте Perplexity API (быстрее и надежнее)
- **Для разработки/тестирования**: локальная модель (бесплатно)
- **Для приватных данных**: только локальная модель
- **При ограниченном интернете**: локальная модель

## Дополнительная информация

- Документация Ollama: https://github.com/ollama/ollama
- Список моделей: https://ollama.ai/library
- Сообщество: https://discord.gg/ollama
