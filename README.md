# Support Assistant

Консольный ассистент службы поддержки на базе LLM с поддержкой MCP инструментов и RAG.

## Функциональность

- 🤖 Интеграция с Perplexity API (модель sonar-pro)
- 🔧 Поддержка MCP (Model Context Protocol) инструментов
- 📚 RAG система для поиска в документации
- 🎫 Автоматическое создание тикетов

## Структура проекта

```
ticketManager/
├── config/
│   ├── mcp_config.yaml        # Конфигурация MCP серверов
│   ├── api_keys.yaml          # API ключи
│   └── local_llm_config.yaml  # Настройки локальной LLM
├── docs/                      # Документация для RAG
├── data/
│   └── embeddings.json        # Индекс эмбедингов
├── src/
│   ├── main.py                # Точка входа
│   ├── llm_client.py          # Клиент Perplexity API
│   ├── mcp_handler.py         # Обработчик MCP
│   ├── rag/
│   │   ├── indexer.py         # Индексация документов
│   │   ├── embeddings.py      # Генерация эмбедингов
│   │   └── retriever.py       # Поиск документов
│   └── prompts/
│       └── system_prompt.py   # Системный промпт
├── requirements.txt
└── README.md
```

## Установка

1. Клонируйте репозиторий
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Настройте конфигурацию:
   - Добавьте API ключ Perplexity в `config/api_keys.yaml`
   - Настройте MCP серверы в `config/mcp_config.yaml`
   - Укажите параметры локальной LLM в `config/local_llm_config.yaml`

## Использование

### Запуск

```bash
cd src
python main.py
```

### Команды

| Команда | Описание |
|---------|----------|
| `/index` | Индексация документов из папки `docs/` |
| `/clear` | Очистка истории диалога |
| `/status <id>` | Проверка статуса тикета |
| `/help` | Справка по командам |
| `/exit` | Выход из программы |

### Работа с RAG

1. Поместите документацию в папку `docs/` (поддерживаются `.txt`, `.md`)
2. Запустите индексацию командой `/index`
3. Ассистент будет автоматически искать в документации при ответах

## Архитектура

### LLM Client
Взаимодействие с Perplexity API для генерации ответов.

### MCP Handler
Обработка вызовов инструментов из ответов LLM:
- `create_ticket` - создание тикета
- `search_knowledge_base` - поиск в документации
- `get_ticket_status` - статус тикета

### RAG Pipeline
1. **Indexer**: Разбиение документов на чанки (500 символов, перекрытие 50)
2. **Embeddings**: Генерация векторных представлений через локальную LLM
3. **Retriever**: Поиск по косинусному сходству (топ-3 результата)

## Требования

- Python 3.9+
- Perplexity API ключ
- Локальная LLM для эмбедингов (рекомендуется Ollama с `nomic-embed-text`)
- MCP сервер для управления тикетами

## Лицензия

MIT
