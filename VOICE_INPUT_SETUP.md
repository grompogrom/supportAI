# Настройка голосового ввода

Этот документ содержит инструкции по установке и тестированию функции голосового ввода.

## Установка зависимостей

### 1. Установка PortAudio (необходимо для PyAudio)

#### macOS:
```bash
brew install portaudio
```

#### Linux (Ubuntu/Debian):
```bash
sudo apt-get install portaudio19-dev python3-pyaudio
```

#### Windows:
PyAudio может быть установлен напрямую через pip (см. следующий шаг).

### 2. Установка Python зависимостей

```bash
pip install -r requirements.txt
```

Или установите зависимости вручную:

```bash
pip install SpeechRecognition>=3.10.0
pip install PyAudio>=0.2.13
```

**Примечание для macOS с Apple Silicon (M1/M2/M3):**
Если возникают проблемы с установкой PyAudio:

```bash
# Установите через Homebrew
brew install portaudio

# Затем установите PyAudio с указанием путей
CFLAGS="-I$(brew --prefix portaudio)/include" \
LDFLAGS="-L$(brew --prefix portaudio)/lib" \
pip install pyaudio
```

## Использование

### Запуск приложения

```bash
cd src
python3 main.py
```

### Использование голосового ввода

1. В консоли введите команду:
   ```
   /voice
   ```

2. Дождитесь сообщения о калибровке микрофона

3. Начните говорить на русском языке

4. Когда закончите, нажмите **Enter**

5. Приложение распознает вашу речь и отправит текст ассистенту

6. Вы увидите распознанный текст и ответ от ассистента

### Пример использования

```
> /voice

==================================================
🎤 Калибровка микрофона... (пожалуйста, подождите)
✓ Готово! Говорите... (нажмите Enter для остановки)

[Вы говорите: "Создай задачу с высоким приоритетом..."]
[Нажимаете Enter]

🔄 Обработка записи...
==================================================

📝 Распознано: Создай задачу с высоким приоритетом для исправления бага в авторизации