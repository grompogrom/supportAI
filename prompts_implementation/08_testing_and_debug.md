# Промпт для комплексного тестирования и дебага

## Цель
Провести полное тестирование приложения Support Assistant после завершения разработки всех модулей.

---

## Часть 1: Предварительная проверка окружения

### 1.1 Проверка зависимостей
```
Перед началом тестирования убедись:

1. Python версия 3.9+
   $ python --version

2. Все зависимости установлены
   $ pip install -r requirements.txt

3. Ollama запущен и модель загружена
   $ ollama list  # должен показать nomic-embed-text
   $ curl http://localhost:11434/api/tags  # проверка API

4. Конфигурационные файлы заполнены
   - config/api_keys.yaml содержит валидный API ключ Perplexity
   - config/local_llm_config.yaml указывает на работающий Ollama
   - config/mcp_config.yaml настроен (MCP сервер опционален для базового теста)
```

### 1.2 Проверка структуры проекта
```
Убедись что все файлы на месте:

ticketManager/
├── config/
│   ├── api_keys.yaml        ✓
│   ├── local_llm_config.yaml ✓
│   └── mcp_config.yaml       ✓
├── data/
│   └── embeddings.json       ✓
├── docs/
│   └── (минимум 1 файл .md или .txt) ✓
├── src/
│   ├── main.py               ✓
│   ├── llm_client.py         ✓
│   ├── mcp_handler.py        ✓
│   ├── rag/
│   │   ├── indexer.py        ✓
│   │   ├── embeddings.py     ✓
│   │   └── retriever.py      ✓
│   └── prompts/
│       └── system_prompt.py  ✓
└── requirements.txt          ✓
```

---

## Часть 2: Модульное тестирование (Unit Testing)

### 2.1 Тестирование EmbeddingGenerator

**Цель**: Проверить генерацию эмбедингов через локальную LLM

**Тест-кейсы**:

| # | Сценарий | Входные данные | Ожидаемый результат |
|---|----------|----------------|---------------------|
| 1 | Базовая генерация | "Привет мир" | Список float, длина > 0 |
| 2 | Пустая строка | "" | Эмбединг или ошибка (зависит от модели) |
| 3 | Длинный текст | Текст 5000+ символов | Эмбединг без ошибки |
| 4 | Спецсимволы | "Test 🎉 <script>" | Эмбединг без ошибки |
| 5 | Батчевая обработка | 10 текстов | 10 эмбедингов одинаковой размерности |
| 6 | Недоступный сервер | host="invalid" | EmbeddingConnectionError |

**Метод проверки**:
```
1. Создай тестовый скрипт в src/
2. Инициализируй EmbeddingGenerator с реальным конфигом
3. Выполни каждый тест-кейс
4. Проверь:
   - Тип результата (list of float)
   - Размерность одинакова для всех текстов
   - Значения в разумном диапазоне (-1 до 1 обычно)
```

### 2.2 Тестирование DocumentIndexer

**Цель**: Проверить индексацию документов

**Подготовка**: Создай тестовые файлы в docs/
- test1.md (100 символов)
- test2.txt (1000 символов)
- test3.md (5000 символов)
- subdir/test4.md (проверка рекурсивного сканирования)

**Тест-кейсы**:

| # | Сценарий | Ожидаемый результат |
|---|----------|---------------------|
| 1 | scan_documents() | Находит все 4 файла |
| 2 | read_document() существующий | Возвращает содержимое |
| 3 | read_document() несуществующий | FileNotFoundError |
| 4 | split_into_chunks() короткий текст | 1 чанк |
| 5 | split_into_chunks() длинный текст | Несколько чанков с перекрытием |
| 6 | index_all() | IndexingResult с корректной статистикой |
| 7 | save_index() и load_index() | JSON сохраняется и загружается |

**Проверка чанкирования**:
```
Текст: 1200 символов, chunk_size=500, overlap=50
Ожидаемые чанки:
- Чанк 0: позиция 0-500
- Чанк 1: позиция 450-950
- Чанк 2: позиция 900-1200

Проверь:
- Перекрытие между чанками = 50 символов
- Последний чанк может быть короче
- Нет потерянных символов
```

### 2.3 Тестирование DocumentRetriever

**Цель**: Проверить поиск по косинусному сходству

**Подготовка**: Проиндексируй документы командой /index

**Тест-кейсы**:

| # | Сценарий | Ожидаемый результат |
|---|----------|---------------------|
| 1 | search() релевантный запрос | Топ результат с высоким score (>0.7) |
| 2 | search() нерелевантный запрос | Низкие scores (<0.5) |
| 3 | search() top_k=1 | Ровно 1 результат |
| 4 | search() top_k=10 (больше чанков) | Все доступные чанки |
| 5 | Косинусное сходство идентичных векторов | Ровно 1.0 |
| 6 | Косинусное сходство ортогональных | Близко к 0.0 |
| 7 | format_results_for_llm() | Читаемый текст с источниками |

**Метод проверки косинусного сходства**:
```
1. Сгенерируй эмбединг для текста A
2. Сгенерируй эмбединг для того же текста A
3. Косинусное сходство должно быть ~1.0 (или очень близко)
4. Сгенерируй эмбединг для совершенно другого текста B
5. Сходство A и B должно быть значительно ниже
```

### 2.4 Тестирование LLMClient

**Цель**: Проверить взаимодействие с Perplexity API

**Тест-кейсы**:

| # | Сценарий | Ожидаемый результат |
|---|----------|---------------------|
| 1 | send_message() простой | Текстовый ответ |
| 2 | send_message() с tool_call в промпте | Ответ с <tool_call> |
| 3 | send_tool_result() | Обновленный ответ |
| 4 | История сохраняется | len(messages) увеличивается |
| 5 | clear_history() | Только системный промпт остается |
| 6 | Неверный API ключ | AuthenticationError |
| 7 | Очень длинное сообщение | Ответ или ошибка лимита |

**Внимание**: Тесты с реальным API расходуют квоту!

### 2.5 Тестирование MCPHandler

**Цель**: Проверить парсинг и форматирование tool calls

**Тест-кейсы парсинга**:

| # | Входной текст | Ожидаемый результат |
|---|---------------|---------------------|
| 1 | `<tool_call>{"tool":"test","parameters":{}}</tool_call>` | ToolCallRequest(tool_name="test") |
| 2 | Текст без tool_call | None |
| 3 | Невалидный JSON в tool_call | None |
| 4 | Несколько tool_call блоков | Первый блок |
| 5 | tool_call с переносами строк | Корректный парсинг |

**Тест-кейсы форматирования**:

| # | Входные данные | Ожидаемый формат |
|---|----------------|------------------|
| 1 | success=True, result={...} | JSON с "success": true |
| 2 | success=False, error="msg" | JSON с "error": "msg" |

**Тест локального инструмента**:
```
1. Зарегистрируй тестовый инструмент через register_local_tool()
2. Вызови call_tool() для этого инструмента
3. Проверь что handler был вызван с правильными параметрами
```

---

## Часть 3: Интеграционное тестирование

### 3.1 Тест RAG Pipeline (end-to-end)

**Сценарий**: Полный цикл от документа до ответа

```
Шаги:
1. Создай документ в docs/test_rag.md:
   "Для сброса пароля перейдите в настройки профиля и нажмите 
   'Изменить пароль'. Введите текущий пароль и новый пароль дважды."

2. Запусти индексацию: /index

3. Выполни поиск: search("как сбросить пароль")

4. Проверь:
   - Результат содержит текст про сброс пароля
   - Similarity score > 0.6
   - Источник указывает на test_rag.md
```

### 3.2 Тест LLM + Tool Calls

**Сценарий**: LLM вызывает инструмент и получает результат

```
Шаги:
1. Отправь сообщение: "Меня зовут Иван, у меня проблема с входом"

2. Ожидай что LLM:
   - Запросит детали проблемы
   - Вызовет create_ticket после получения деталей
   - Вызовет search_knowledge_base для поиска решения

3. Проверь:
   - tool_call корректно распарсен
   - Результат tool отправлен обратно в LLM
   - Финальный ответ содержит информацию из RAG
```

### 3.3 Тест консольного интерфейса

**Тест-кейсы команд**:

| Команда | Ожидаемое поведение |
|---------|---------------------|
| `/index` | Индексация запускается, выводится статистика |
| `/clear` | История очищена, подтверждающее сообщение |
| `/status TKT-123` | Запрос статуса (ошибка если MCP недоступен) |
| `/help` | Вывод справки |
| `/exit` | Завершение программы |
| `/unknown` | Сообщение о неизвестной команде |
| Пустой ввод | Игнорируется |
| Ctrl+C | Корректное завершение |

---

## Часть 4: Сценарное тестирование (User Journey)

### 4.1 Сценарий: Новый пользователь

```
Шаги пользователя:
1. Запустить программу
2. Ввести приветствие
3. Представиться (имя)
4. Описать проблему
5. Получить номер тикета
6. Получить ответ на основе документации

Проверяемые аспекты:
- Ассистент приветствует и спрашивает имя
- Ассистент уточняет проблему
- Создается тикет (tool_call)
- Выполняется поиск в RAG (tool_call)
- Ответ содержит релевантную информацию
- Сообщается номер тикета
```

### 4.2 Сценарий: Повторный вопрос

```
Шаги:
1. Задать вопрос, получить ответ
2. Задать уточняющий вопрос

Проверить:
- История диалога сохраняется
- Контекст предыдущих сообщений учитывается
```

### 4.3 Сценарий: Вопрос вне базы знаний

```
Шаги:
1. Задать вопрос, на который нет ответа в docs/

Проверить:
- Ассистент честно сообщает об отсутствии информации
- Предлагает альтернативу (связаться с оператором)
- НЕ выдумывает информацию
```

---

## Часть 5: Негативное тестирование

### 5.1 Тесты отказоустойчивости

| Сценарий | Действие | Ожидаемое поведение |
|----------|----------|---------------------|
| Ollama не запущен | Запустить /index | Понятное сообщение об ошибке |
| Неверный API ключ | Отправить сообщение | AuthenticationError с описанием |
| Пустая папка docs/ | Запустить /index | Сообщение "нет документов" |
| Поврежденный embeddings.json | Выполнить поиск | IndexCorruptedError |
| Очень длинный ввод | 10000+ символов | Обработка или лимит |
| Спецсимволы в запросе | SQL injection, XSS | Безопасная обработка |

### 5.2 Тесты граничных условий

| Условие | Проверка |
|---------|----------|
| Документ 0 байт | Пропускается при индексации |
| Документ 1 символ | Создается 1 чанк |
| 1000 документов | Индексация завершается за разумное время |
| Чанк ровно 500 символов | Корректное разбиение |
| Запрос из 1 слова | Поиск работает |
| Запрос из 100 слов | Поиск работает |

---

## Часть 6: Тестирование производительности

### 6.1 Метрики для измерения

| Операция | Целевое время | Метод измерения |
|----------|---------------|-----------------|
| Генерация 1 эмбединга | < 1 сек | time.time() |
| Индексация 10 документов | < 30 сек | time.time() |
| Поиск в 100 чанках | < 0.5 сек | time.time() |
| Ответ LLM | < 10 сек | time.time() |
| Запуск приложения | < 5 сек | time.time() |

### 6.2 Профилирование памяти

```
Проверь потребление памяти:
1. При загрузке большого индекса (1000+ чанков)
2. При длинной истории диалога (50+ сообщений)
3. Убедись в отсутствии утечек памяти при повторных операциях
```

---

## Часть 7: Чек-лист финальной проверки

### Функциональность
- [ ] Приложение запускается без ошибок
- [ ] Команда /index индексирует документы
- [ ] Команда /clear очищает историю
- [ ] Команда /help показывает справку
- [ ] Команда /exit корректно завершает работу
- [ ] Диалог с LLM работает
- [ ] Tool calls парсятся и выполняются
- [ ] RAG возвращает релевантные результаты
- [ ] Тикет создается (если MCP сервер доступен)

### Обработка ошибок
- [ ] Отсутствующий конфиг - понятное сообщение
- [ ] Недоступный Ollama - понятное сообщение
- [ ] Неверный API ключ - понятное сообщение
- [ ] Пустой индекс - предложение запустить /index
- [ ] Ctrl+C - корректное завершение

### Качество кода
- [ ] Нет необработанных исключений
- [ ] Логи информативны
- [ ] Нет хардкода путей (используются конфиги)
- [ ] Типизация соответствует сигнатурам

---

## Часть 8: Процедура дебага

### При обнаружении ошибки:

1. **Локализация**
   ```
   - В каком модуле ошибка? (traceback)
   - При каком вводе возникает?
   - Воспроизводится ли стабильно?
   ```

2. **Изоляция**
   ```
   - Протестируй модуль отдельно
   - Убери зависимости (mock)
   - Найди минимальный воспроизводящий пример
   ```

3. **Диагностика**
   ```
   - Добавь print/logging в подозрительные места
   - Проверь типы и значения переменных
   - Сравни с ожидаемым поведением из промпта
   ```

4. **Исправление**
   ```
   - Внеси минимальное изменение
   - Проверь что исходная ошибка исправлена
   - Проверь что не сломалось другое (регрессия)
   ```

### Типичные проблемы и решения

| Симптом | Вероятная причина | Решение |
|---------|-------------------|---------|
| Connection refused (Ollama) | Ollama не запущен | `ollama serve` |
| 401 Unauthorized | Неверный API ключ | Проверить api_keys.yaml |
| Empty embedding | Пустой текст на вход | Добавить валидацию |
| tool_call не парсится | Неверный формат JSON | Проверить регулярку |
| Низкий similarity | Плохое качество эмбедингов | Попробовать другую модель |
| IndexNotFoundError | Не выполнена индексация | Запустить /index |
| KeyError в конфиге | Неполный YAML | Сверить со структурой |

---

## Артефакты тестирования

После завершения тестирования должны быть:

1. **Отчет о тестировании** - результаты всех тест-кейсов
2. **Список багов** - найденные проблемы с приоритетами
3. **Метрики производительности** - замеры времени операций
4. **Скриншоты/логи** - примеры работы приложения
